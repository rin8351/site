version: '3'

services:
  web:
    restart: always
    build: .
    # command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/usr/src/app
    ports:
      - "443:443"
    environment:
      - DEBUG=1